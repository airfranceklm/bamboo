# ---------------------------------------------------------------
# Monit configuration file generated by application_procfile for:
# bamboo-agent
# ---------------------------------------------------------------

check process bamboo with pidfile "#{node[:bamboo][:data_dir]}/bin/bamboo-agent.pid"
start program "/bin/bash -c 'PID_FILE=#{node[:bamboo][:data_dir]}/bin/bamboo-agent.pid /etc/init.d/bamboo-agent start'" with timeout 60 seconds
stop program  = "/bin/bash -c 'PID_FILE=#{node[:bamboo][:data_dir]}/bin/bamboo-agent.pid /etc/init.d/bamboo-agent stop'"